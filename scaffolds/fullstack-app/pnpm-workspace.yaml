packages:
  - "apps/*"
  - "packages/*"

# pnpm blocks dependency build scripts by default; allow only the ones that need them.
allowBuilds:
  "@sentry/cli": true
  "@tailwindcss/oxide": true
  esbuild: true
  msw: true
  protobufjs: true
  sharp: true
